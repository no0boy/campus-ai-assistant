# 校园AI知识平台 → 企业级 AI Agent 平台 升级方案

> 高级AI架构师 · 架构设计文档

---

## 一、现状评估

### 已有资产（不推翻，只增强）

| 模块 | 成熟度 | 评价 |
|------|:--:|------|
| RAG 检索 | ⭐⭐⭐⭐ | 混合检索 + RRF，企业级水平 |
| 多 Agent 路由 | ⭐⭐⭐⭐ | 5 Agent + 关键词分发 + 模型切换 |
| Think-Act 循环 | ⭐⭐⭐⭐ | 自主决策 + 工具调用，核心壁垒 |
| Token 成本 | ⭐⭐⭐⭐⭐ | 追踪/缓存/压缩，生产级 |
| 运营看板 | ⭐⭐⭐ | 够用，可增强 |
| 用户画像 | ⭐⭐⭐ | 基础有，需深化 |
| 权限体系 | ⭐⭐ | 只有角色字段，缺 RBAC |
| 日志审计 | ⭐⭐ | usage_logs 可用，缺结构化 |

### 结论

**核心 AI 能力已达企业级 70%，差的是平台化最后一公里：多渠道、权限、审计、编排。**

---

## 二、目标系统架构

```
┌────────────────────────────────────────────────────────────────┐
│                        接入层（多渠道）                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │ Web 网页  │  │ 飞书 Bot  │  │ 微信 Bot  │  │ Open API     │  │
│  │ (已有)   │  │ (新增)   │  │ (预留)   │  │ (新增)       │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────┬───────┘  │
│       │             │             │               │           │
│       └─────────────┼─────────────┼───────────────┘           │
│                     │             │                            │
│              ┌──────┴─────────────┴──────┐                     │
│              │    OpenClaw（Agent 网关）   │   ← 新增核心        │
│              │    · 消息路由 · 鉴权 · 限流 │                     │
│              └────────────┬──────────────┘                     │
│                           │                                     │
├───────────────────────────┼─────────────────────────────────────┤
│                           │           网关层                    │
│              ┌────────────┴──────────────┐                     │
│              │     API Gateway            │                     │
│              │  · RBAC 鉴权 · 限流 · 日志 │                     │
│              └────────────┬──────────────┘                     │
│                           │                                     │
├───────────────────────────┼─────────────────────────────────────┤
│                           │           Agent 编排层               │
│  ┌────────────────────────┼──────────────────────────────┐     │
│  │              Agent 工作流引擎（新增）                     │     │
│  │  ┌─────────┐  ┌──────────┐  ┌────────────────────┐   │     │
│  │  │ 意图路由  │→│ Think-Act│→│  工具调用（Tool Call）│   │     │
│  │  │ (已有)   │  │ 循环(已有)│  │  课表/成绩/通知(新增) │   │     │
│  │  └─────────┘  └──────────┘  └────────────────────┘   │     │
│  └───────────────────────────────────────────────────────┘     │
│                           │                                     │
├───────────────────────────┼─────────────────────────────────────┤
│                           │            服务层                    │
│  ┌──────┐ ┌──────┐ ┌─────┐┼─────┐ ┌──────┐ ┌────────┐       │
│  │ RAG  │ │ 缓存  │ │用户 ││Token │ │ 日志  │ │ 通知    │       │
│  │ 检索  │ │ LRU  │ │画像 ││监控  │ │ 审计  │ │ 推送    │       │
│  │(已有)│ │(已有)│ │(已有)││(已有)│ │(增强)│ │(新增)  │       │
│  └──────┘ └──────┘ └─────┘└─────┘ └──────┘ └────────┘       │
│                           │                                     │
├───────────────────────────┼─────────────────────────────────────┤
│                           │            数据层                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ SQLite   │  │ ChromaDB │  │ BM25     │  │ 缓存层    │      │
│  │ 业务数据  │  │ 向量存储  │  │ 关键词    │  │ Redis    │      │
│  │ (已有)   │  │ (已有)   │  │ (已有)   │  │ (预留)   │      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
└────────────────────────────────────────────────────────────────┘
```

---

## 三、数据库升级设计

### 新增表（3张）

**rbac_roles** — 角色权限

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| name | VARCHAR(32) | admin / teacher / student / guest |
| permissions | JSON | {"chat":true, "admin":false, "api":false, "webhook":false} |

**audit_logs** — 审计日志

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| user_id | INT | |
| action | VARCHAR(64) | chat / login / upload / webhook / admin |
| ip_address | VARCHAR(45) | |
| user_agent | VARCHAR(256) | |
| detail | JSON | 操作详情 |
| created_at | DATETIME | |

**tool_registry** — 工具注册表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| name | VARCHAR(64) | get_course / get_grade / get_notice |
| display_name | VARCHAR(128) | 查询课表 / 查询成绩 / 最新通知 |
| endpoint | VARCHAR(256) | API 地址或函数名 |
| params_schema | JSON | 参数定义 |
| enabled | INT | 0/1 |
| created_at | DATETIME | |

### 修改现有表

**users 表**：加 `rbac_role_id` 外键，关联 rbac_roles

**usage_logs 表**：加 `ip_address`、`user_agent`、`request_id`

---

## 四、OpenClaw 接入方案

### 定位

OpenClaw = **消息网关**，不处理 AI 逻辑。

```
飞书/微信 → OpenClaw（消息收/发） → 你的后端（AI 处理）
```

### 配置结构

```json
{
  "backend": {
    "url": "https://你的HF空间/api/webhook/chat",
    "auth": "Bearer openclaw-key",
    "stream": true
  },
  "channels": {
    "feishu": { "appId": "...", "appSecret": "..." }
  }
}
```

### 你已经加了什么

`routes/webhook.py` — 统一消息入口，处理 `POST /api/webhook/chat`，含限流+鉴权+流式响应。OpenClaw 配好后即插即用。

---

## 五、飞书接入方案

### 三步走

```
1. 飞书开放平台 → 创建企业自建应用
2. 配置机器人 → 获取 App ID + App Secret
3. OpenClaw 填入凭证 → 指向你的 webhook → 完成
```

### 消息流

```
飞书群 @机器人 "查课表"
  → 飞书服务器 → OpenClaw 收消息
  → POST 你的 /api/webhook/chat
  → agent_ask() → Tool Call → 返回
  → OpenClaw → 飞书回复
```

---

## 六、Tool Calling 设计

### Agent 工具箱（扩展）

```
已有：
  ├── search_knowledge()     查知识库
  ├── get_weather()          天气查询
  └── search_web()           网页搜索

新增：
  ├── get_course()           查课表 (模拟教务API)
  ├── get_grade()            查成绩
  ├── get_notice()           查通知公告
  └── get_calendar()         校历查询
```

### 工具调用流程

```
用户："我明天有什么课"
  → Agent Think: "需要查课表"
  → TOOL: 查课表 明天
  → get_course("明天") → "8:00 数据结构, 10:00 操作系统"
  → Agent Think: "信息够了"
  → ANSWER: "你明天有两节课：8:00数据结构，10:00操作系统"
```

### 为什么用模拟数据

真实教务系统 API 通常不对外开放。用预设 JSON 数据模拟，面试时坦率说"目前用模拟数据，对接真实 API 只需改 endpoint"——这反而是工程意识的体现。

---

## 七、RBAC 权限设计

### 角色矩阵

| 权限 | admin | teacher | student | guest |
|------|:--:|:--:|:--:|:--:|
| Web 聊天 | ✅ | ✅ | ✅ | ✅ |
| 飞书接入 | ✅ | ✅ | ✅ | ❌ |
| 管理后台 | ✅ | ✅ | ❌ | ❌ |
| API 调用 | ✅ | ✅ | ❌ | ❌ |
| 知识库管理 | ✅ | ❌ | ❌ | ❌ |
| 用户管理 | ✅ | ❌ | ❌ | ❌ |

### 实现方式

- 中间件拦截所有 `/api/*` 请求
- 从 JWT 中取 role → 查权限表
- 无权限返回 403

---

## 八、Agent 工作流编排

### 当前状态

Agent 循环是代码写死的 Think-Act 流程。够用，但不可配置。

### 升级为可配置工作流

管理员后台配置预设工作流：

```
工作流模板：奖学金咨询
  ├── 触发：用户提到"奖学金"
  ├── Step1：查奖学金知识库
  ├── Step2：查用户画像(年级/专业)
  ├── Step3：匹配适用奖学金 → 生成个性化回答
  └── Step4：推荐相关文档
```

### 实现方案

- 工作流配置存入 `tool_registry` 表（JSON 字段）
- 后端 `workflow_engine` 模块根据配置执行
- 不引入重量级框架，自己实现轻量编排器

---

## 九、7天可落地实施计划

| 天数 | 任务 | 产出 |
|:--:|------|------|
| Day1 | RBAC 表 + 权限中间件 | 角色权限生效 |
| Day2 | Tool Calling 工具注册表 + 4个模拟工具 | Agent 能查询课表/成绩 |
| Day3 | 审计日志表 + 中间件 | 所有操作可追溯 |
| Day4 | 工作流引擎（配置读取+执行） | 管理员可配置工作流 |
| Day5 | OpenClaw + 飞书打通 | 飞书群能@机器人聊天 |
| Day6 | 管理后台增强（RBAC/工具/工作流配置页） | 图形化管理 |
| Day7 | 联调测试 + 文档更新 | 完整可演示 |

### 每日产出约 100-150 行代码，不改动现有架构，纯增量叠加。

---

## 十、面试价值矩阵

| 功能 | 面试能说的 |
|------|-----------|
| RBAC | "从单角色升级到权限矩阵，admin/teacher/student 三层隔离" |
| 审计日志 | "全链路可追溯：谁、什么时候、做了什么、结果如何" |
| Tool Calling | "Agent 不是只能聊天，能调教务系统查课表成绩" |
| 工作流引擎 | "管理员可配置 Agent 的行为链，不需要改代码" |
| 多渠道 | "Web + 飞书双入口，一套 Agent 后端服务所有端" |
