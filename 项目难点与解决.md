# 项目难点与解决方案

## 难点一：纯向量检索召回不准

**问题**：用户问"宿舍几点关门"，向量检索把"关门时间"匹配成语义相近的不相关内容。

**解决**：做混合检索。Dense向量 + BM25关键词双路召回，用RRF算法融合排序。向量懂语义，BM25懂精确词，互补。引入jieba中文分词和rank_bm25，纯Python零外部依赖。

---

## 难点二：多轮对话Token膨胀

**问题**：用户连续问10轮，每轮都把完整历史塞进Prompt，Token越来越贵。

**解决**：对话摘要压缩。超过3000 token时，用便宜模型(qwen-turbo)把早期对话压成摘要，只保留最近2轮完整内容。加上LRU问题缓存，相同问题零消耗。

---

## 难点三：用户意图分散，一个大Prompt不够

**问题**：校园问题跨度大——奖学金、宿舍、选课、社团。一个通用Prompt检索不准。

**解决**：拆成5个专属Agent。每个配独立知识库+专属Prompt+专属模型。关键词路由分发，零额外API调用。奖学金用qwen-plus够用，宿舍制度用qwen3.6-plus推理强，社团流程用qwen-turbo省钱。

---

## 难点四：Agent只是"死"的工作流

**问题**：固定流程的问→搜→答，遇到复杂问题(对比两份文档)不会自主多步推理。

**解决**：实现Think-Act循环。LLM自己决定查什么、查几次、要不要调外部工具、够了再输出。加外部工具(天气/搜索)，Agent能走出知识库取信息。

---

## 难点五：成本黑洞

**问题**：LLM每调一次都花钱，但不知道花在哪、花多少。

**解决**：tiktoken本地估算+模型定价匹配，每次调用记录Token数/成本/响应时间。运营看板实时监控，按日/模型维度下钻。P95响应时间、缓存命中率全可视化。

---

## 面试时怎么讲

"最大的难点是让Agent从固定流程变成自主决策。我先做了5个Agent的关键词路由，发现还是不够灵活，于是加了Think-Act循环让LLM自己决定查什么、查几次、何时输出。过程中还遇到Token膨胀和检索不准的问题，分别用上下文压缩和混合检索解决了。"
