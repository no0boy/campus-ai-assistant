# 校园 AI 助手 — 面试准备文档

## 项目一句话

从零搭建的校园 RAG 智能问答系统，包含多 Agent 协作、混合检索、Token 成本控制、用户画像和智能推荐，已上线可体验。

---

## 核心亮点（面试必讲 5 点）

### 1. 混合检索（Dense + BM25 + RRF 融合）
- 向量语义检索 + BM25 关键词检索 双路召回
- RRF 算法融合排序，互补提升召回率
- `backend/services/hybrid_search.py`

### 2. 多 Agent 架构 + Think-Act 循环
- 5 个专属 Agent（奖学金/宿舍/社团/选课/总助手）
- 关键词路由 + 模型自动切换
- Agent 模式：LLM 自主决定"查几次、何时答"
- `config.py` AGENTS 配置 + `rag_service.py` agent_ask()

### 3. Token 成本控制
- tiktoken 本地估算 Token 数，匹配模型价格表
- LRU 问题缓存（500条/24h），相同问题零消耗
- 对话上下文压缩：超长对话自动摘要
- `services/token_tracker.py` + `rag_service.py` QueryCache

### 4. 运营可观测性
- 每次 LLM 调用记录 Token/成本/响应时间
- 数据看板：Token 趋势 + 模型分布 + P50/P95/P99
- 热门问题 Top 10 + 文档引用统计
- `routes/stats.py` dashboard API

### 5. 用户画像 + 智能推荐
- 首次登录引导采集年级/专业
- 长期记忆：每 5 轮对话自动摘要
- 千人千面推荐：年级 + 专业 + 历史 → 预测问题
- `routes/user.py` + `database.py` User 表

---

## 面试高频追问速查

| 面试官问 | 你答 |
|----------|------|
| 为什么选 ChromaDB 不用 Pinecone？ | 轻量嵌入式，零运维，适合校园数据规模 |
| 混合检索怎么验证效果好？ | 对比纯向量/纯 BM25/混合，混合召回覆盖更全 |
| Agent 怎么防止无限循环？ | 最多 3 轮，加上 Token 总量限制 |
| 缓存什么时候失效？ | 24h TTL + 知识库更新时手动清空 |
| SQLite 扛得住吗？ | 当前够用，改一行配置可切 MySQL/PostgreSQL |
| API Key 怎么保护？ | .gitignore + HF Space Secrets，代码里只有空模板 |

---

## 技术栈速记

```
后端：Python + FastAPI + LangChain + ChromaDB + SQLite
AI：  Qwen 系列 / Claude / DeepSeek / Gemini（OpenAI 兼容层统一）
向量：DashScope text-embedding-v3
分词：jieba + BM25 (rank_bm25)
前端：HTML + CSS + Vanilla JS + Chart.js CDN
部署：Docker + HuggingFace Space
```

---

## 演示链接

- 在线：https://no0boy2026-campus-ai-assistant.hf.space/app/student/login.html
- 代码：https://github.com/no0boy/campus-ai-assistant
