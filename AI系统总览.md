# 校园AI知识平台 — 系统总览

## 一句话概括

**具备自主决策、工具调用、长期记忆的多 Agent 协作知识平台。**

---

## 整体架构（从上到下看）

```
┌─────────────────────────────────────────────────────┐
│                   前端（用户界面）                     │
│  登录 → 一键体验学生端 / 管理后台                      │
│  聊天页 → 侧边栏 Agent 开关 → 对话气泡 + Agent 卡片    │
└────────────────────┬────────────────────────────────┘
                     │ HTTP/SSE
┌────────────────────┴────────────────────────────────┐
│               FastAPI 后端（路由层）                   │
│  /api/auth    登录/注册（JWT 鉴权）                    │
│  /api/chat    问答接口（普通/Agent/流式）               │
│  /api/documents  知识库管理（上传/预览/批量删）         │
│  /api/stats   运营看板数据                             │
│  /api/usage   Token 消耗统计                          │
│  /api/user    用户画像 + 推荐                          │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────┴────────────────────────────────┐
│                 服务层（AI 核心）                      │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ Agent路由 │→│  Think-  │→│  工具调用          │  │
│  │ 5个Agent  │  │  Act循环  │  │  知识库/天气/搜索  │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ 混合检索  │  │ Token追踪 │  │  用户画像+记忆    │  │
│  │Dense+BM25│  │+成本控制  │  │  个性化推荐       │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────┴────────────────────────────────┐
│                 数据层                                │
│  SQLite(4张表) + ChromaDB(向量库) + BM25(关键词索引)    │
│  users / conversations / documents / usage_logs       │
└─────────────────────────────────────────────────────┘
```

---

## 一个请求的完整旅程

```
用户点击 Agent 模式 → 输入"明天广州天气怎么样，适合开运动会吗？"

1. 前端 apiChatAgent() → POST /api/chat/agent/stream
2. 后端 chat_agent_stream() → agent_ask()
3. Agent Think（第1轮）：
   "需要查天气，再查知识库有没有运动会规定"
   → TOOL: 天气查询 广州
4. 工具返回："广州天气：晴 22-28°C"
5. Agent Think（第2轮）：
   "天气适合，再查运动会的校园规定"
   → SEARCH: 运动会 举办条件 操场
6. 知识库返回：学生手册相关条款
7. Agent Think（第3轮）：
   "信息足够了，生成回答"
   → ANSWER: "明天广州晴天22-28°C，非常适合运动会！
      根据学生手册，户外活动需提前报备校团委。
      建议尽快提交申请！"
8. SSE 流式推给前端 → 逐字显示
9. 保存对话 + 写入 Token 日志 + 更新用户记忆
```

---

## 核心模块一句话

| 模块 | 做什么 | 面试话术 |
|------|--------|---------|
| 混合检索 | Dense向量 + BM25关键词 双路召回→RRF融合 | "向量懂语义，BM25懂关键词，两者互补" |
| Agent路由 | 5个Agent，关键词匹配→专属模型+知识库 | "拆 Agent 缩小检索范围，提高准确率" |
| Think-Act | LLM自主决定查什么、查几次、是否调工具 | "不是写死的流程，让 LLM 自己规划步骤" |
| 工具调用 | 天气/搜索，Agent Think时自主调用 | "Agent 能走出知识库，调外部 API" |
| Token控制 | 缓存+压缩+实时追踪+成本估算 | "不是调完不管，每分钱都算清楚" |
| 用户画像 | 对话式采集年级专业，长期记忆 | "千人千面，知道你是谁、关心什么" |
| 运营看板 | 6图表+P95，Token/模型/RAG全维度 | "不是黑盒，一切可观测" |

---

## 面试3分钟讲法

**开场（30秒）：**
"我做了一个具备自主决策和工具调用能力的多 Agent 校园知识平台。它不只是简单的问答机器人，而是能像实习生一样——收到任务后自己规划步骤、查资料、调外部工具、最后给出完整的回答。已上线可体验，GitHub 开源。"

**核心技术（2分钟，挑3个讲）：**

"第一个亮点是混合检索。纯向量检索懂语义但漏关键词，纯BM25懂关键词但不懂语义，我把两路结果用 RRF 融合，召回率明显提升。"

"第二个是多 Agent 架构。我拆了 5 个专属 Agent，每人配不同的模型和知识库。宿舍问题用 qwen3.6-plus 推理，社团问题用 qwen-turbo 省钱。不是一个大 Prompt 硬扛。"

"第三个是 Think-Act 循环。LLM 自己思考、决定下一步做什么、调用工具、拿到结果再思考、满意了才输出。就像人解决问题一样，不是一条线走到底。"

**结尾（30秒）：**
"这个项目我从零搭的，前端后端 AI 部署全自己搞。过程中解决了 Token 成本控制、缓存策略、用户画像、长期记忆这些实际问题。每个模块的设计我都能讲清楚为什么这么做。"
